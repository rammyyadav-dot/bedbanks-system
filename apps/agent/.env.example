# Public Agent API base URL only; never place credentials in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_AGENT_API_URL=http://localhost:3001/api/v1
# Sample inventory is available only in development and preview builds.
# Production builds ignore this flag even when set to true.
NEXT_PUBLIC_ENABLE_DEMO_INVENTORY=false
