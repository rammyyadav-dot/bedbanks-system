# Connector records

Create `docs/connectors/<provider>/` only when a supplier is approved. Store documentation, quirks, mapping ownership and certification notes here. Store secret references, never credentials.
