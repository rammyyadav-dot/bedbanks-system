{
  "name": "@bedbanks/platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter @bedbanks/agent-portal dev",
    "type-check": "pnpm -r --if-present type-check",
    "lint": "pnpm -r --if-present lint"
  }
}
